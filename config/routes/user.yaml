register_newsletter:
  path: /{_locale}/newsletter
  controller: App\Controller\UserController::newsletter
  requirements:
    _locale: '%app_locales%'
  defaults:
    _locale: '%locale%'
  methods: [GET, POST]

dashboard:
  path: /{_locale}/tableau-de-bord
  controller: App\Controller\UserController::dashboard
  requirements:
    _locale: '%app_locales%'
  defaults:
    _locale: '%locale%'

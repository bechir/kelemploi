{% extends "base.html.twig" %}
{% block title %}{{ 'error.err500.title'|trans }} | {{ website.name }}{% endblock %}

{% block main %}
    <div class="padding-top-80 pb-5 alice-bg">
      <div class="container">
        <div class="row">
          <div class="col">
            <div class="py-5 error-page-wrap text-center white-bg">
              <div class="icon mb-0">
                <img src="{{ asset('images/error.png') }}" class="img-fluid" alt="">
              </div>
              <h1>500</h1>
              <p>Une erreur s'est produite au niveau du serveur.</p>
              <a href="{{ path('index') }}" class="button">Aller à l'accueil</a>
            </div>
          </div>
        </div>
      </div>
    </div>
{% endblock %}

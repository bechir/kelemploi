{% extends 'admin/base.html.twig' %}

{% block left_sidebar %}

{% if active is not defined %}
    {% set active = 'settings' %}
{% endif %}

{% include 'admin/common/left-sidebar.twig' with { active: active } %}

{% endblock %}

{% block main %}
    <div class="panel panel-headline panel-primary">
        <div class="panel-heading py-3">
            <h4 class="my-0 font-15"><span class="lnr lnr-cog fa-lg"></span> Paramètres</h4>
        </div>
        <div class="panel-body px-0 pt-0">
            <ul class="nav nav-tabs">
                <li role="presentation" {% if active == 'featured' %}class="active"{% endif %}><a href="{{ path('admin_settings_featured') }}"><span class="lnr lnr-star fa-lg"></span> Offres à la une</a></li>
                <li role="presentation" {% if active == 'resume-of-week' %}class="active"{% endif %}><a href="{{ path('admin_settings_resume_of_week') }}"><span class="lnr lnr-pushpin fa-lg"></span> CV de la semaine</a></li>
                <li role="presentation" {% if active == 'banner-ad' %}class="active"{% endif %}><a href="{{ path('admin_settings_banner_ad') }}"><span class="lnr lnr-layers fa-lg"></span> Bannière publicitaire</a></li>
                <li role="presentation" {% if active == 'testimonials' %}class="active"{% endif %}><a href="{{ path('admin_settings_testimonials') }}"><span class="fa fa-comments-o fa-lg"></span> Témoignages</a></li>
                {# <li role="presentation" {% if active == 'emails' %}class="active"{% endif %}><a href="{{ path('admin_settings_emails') }}"><span class="lnr lnr-envelope fa-lg"></span> Envoie d'e-mails</a></li> #}
                <li role="presentation" {% if active == 'admin-users' %}class="active"{% endif %}><a href="{{ path('admin_settings_admin_users') }}"><span class="lnr lnr-users fa-lg"></span> Administrateurs</a></li>
            </ul>
            <div class="tab-content">
                <div class="tab-pane active px-0" id="{{ active }}" role="tabpanel" aria-labelledby="{{ active }}-tab">
                    {% block content %}
                    {% endblock %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

<div class="table-responsive">
    {% if articles is not empty %}
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>Titre</th>
                <th>Date de modification</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for article in articles %}
                <tr class="td-3rem">
                    <td><a href="{{ path('admin_blog_article_show', {slug: article.slug}) }}">{{ article.title }}</a></td>
                    <td>{{ article.updatedAt|date('d M, Y') }}</td>
                    <td>
                        {% if article.isArchived %}
                        <label class="label label-default">Archivé</label>
                        {% else %}
                            {% if article.isActivated %}
                                <label class="label label-success">Activé</label>
                            {% else %}
                                <label class="label label-warning">Desactivé</label>
                            {% endif %}
                        {% endif %}
                    </td>
                    <td>
                        <a href="{{ path('blog_article_show', {slug: article.slug}) }}" target="_blank" class="text-info"><i class="fa fa-lg fa-external-link"></i></a>
                        {# <a href="#" data-id="{{ article.id }}" class="disable text-warning mx-3 {% if not article.isActivated %}hidden{% endif %}"><span class="lnr lnr-circle-minus"></span> Désactiver</a>
                        <a href="#" data-id="{{ article.id }}" class="enable text-success mx-3 {% if article.isActivated %}hidden{% endif %}"><span class="lnr lnr-checkmark-circle"></span> Activer</a> #}
                        <a href="{{ path('admin_blog_article_archive', {slug: article.slug}) }}" class="text-archive ml-3"><span class="fa fa-archive"></span> Archiver</a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    {% else %}
        <p class="alert alert-info">Pas d'articles.</p>
    {% endif %}
<div>